git init : ira iniciar um repositorio no diretorio onde o comando foi executado, ou seja, cria uma pasta escondida chamada git

git add . ou arquivo : adiciona as modificacoes nos documentos antes de dar o commit

git commit - m : adiciona as mudanças de maneira armazenada com uma mensagem atrelada ao commit

git push : 

git -v : consulta a versao do git 

git log : listar os commits realizados

git config --global user.name "Name"
git config --global user.email "Email@email.com"